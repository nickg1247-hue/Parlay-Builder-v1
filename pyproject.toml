[project]
name = "parlay-builder-v1"
version = "0.1.0"
description = "MLB-first sports prediction and parlay tool (local dev)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
