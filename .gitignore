# Secrets — never commit these
.env
.env.*
!.env.example
*.pem
credentials.json
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Data — raw ingest scratch stays local; processed artifacts tracked for VPS deploy
data/raw/*
!data/raw/.gitkeep
# Prop line cache is always rebuilt locally / on VPS — never ship stale lines via git.
data/processed/props_repository/**
!data/processed/props_repository/.gitkeep
*.csv
!app/data/park_factors.csv
!data/processed/**/*.csv
*.parquet
!data/processed/**/*.parquet
*.db
!data/parlay_builder.db
*.sqlite
!data/parlay_builder.sqlite

# Notebooks
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Hostinger file-flag for public maintenance mode (presence = ON)
maintenance.on
static/maintenance.on
data/processed/maintenance.on

# Node (if you add a frontend later)
node_modules/
dist/
.next/
